make exe
	pyinstaller -i assets/ship.ico --onefile client.py constants.py DBmanager.py game.py gui.py protocol.py role.
	pyinstaller -i assets/ship.ico --onefile client.pyw client_packet_received.py constants.py draw.py game.py gui.py handle_gui_event.py handle_pygame_event.py map_maker.py protocol.py role.py

frameworks used
	twisted(event driven network engine)
	
	pygame(game engine)
	pygame_gui(GUI engine)

protocols
    client sent:
        register            account password
        login               account password
        create_new_role     name

        change_map          map_name
        try_to_fight_with   enemy_name
        exit_battle(if no loser only) V

        move                direction(maybe change to start_move x y direction)
        shoot_ship          my_ship_id target_ship_id

to do

    # basic functions for most important buildings
        # market
            buy (OK)
            sell (OK)

        # dry dock
            buy (OK)
            sell (OK)
            repair

        # bar
            hire crew (OK)
            fire crew (OK)

        # harbor
            load supply (OK)
            unload supply (OK)







