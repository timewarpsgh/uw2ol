frameworks used
	twisted(event driven network engine)
	
	pygame(game engine)
	pygame_gui(GUI engine)

protocols
    client sent:
        register            account password
        login               account password
        create_new_role     name

        move                direction(maybe change to start_move x y direction)
        change_map          map_name

        try_to_fight_with   enemy_name
        exit_battle


to do
    see others appear and disapear in range(same map)
        login V
        log out V
        change_map

